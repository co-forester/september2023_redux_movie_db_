import React from 'react';

import css from './MoviesList.module.css'

const MoviesList = () => {
    return (
        <div className={css.MoviesList}>
            MoviesList
        </div>
    );
};

export {MoviesList};